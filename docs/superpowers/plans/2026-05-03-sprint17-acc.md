# Sprint 17 — Active Coordination Context (ACC) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar uma camada cognitiva de Active Coordination Context (ACC) ao TOM para resolução de pronomes, comandos elípticos e múltiplos requests abertos sem perguntar demais.

**Architecture:** Nova função `buildActiveCoordinationContext(collab)` no engine que constrói um bloco priorizado (até 500 chars) com FOCUS_CANDIDATE + FOCUS_CONFIDENCE. Bloco injetado no system prompt via `opts.coordContext` (mesmo padrão de `opts.coordHint` da Sprint 16 F4). Skill TOM ganha tabela de heurísticas pra consumir o bloco. Defense-in-depth: strip de cabeçalho duplicado em `applyCoordinationRequestAction` antes do envio. Reforço temporal: linha "Esta semana" no system.js. Zero schema novo, zero migrations, zero PWA.

**Tech Stack:** Node.js (engine, dispatcher, system prompt), markdown (skill TOM). Supabase MCP só para queries de verificação.

**Spec:** `docs/superpowers/specs/2026-05-03-sprint17-acc-design.md`

**Note:** Workflow obrigatório de deploy (Sprint 15 estabelecido):
1. Edit local em `D:/la-organizer/_remote/...`
2. Verificar com `node -c`
3. Clone temp do main, copy files, commit, push origin main (FONTE DA VERDADE)
4. `ssh tom "cd /opt/LA-Organizer && git pull && pm2 restart tom"`
5. Cleanup clone temp
NUNCA scp direto.

---

## Codebase Context

### Funções existentes reutilizadas (não tocar — Sprint 16)

| Função | Arquivo | Linha exata |
|---|---|---|
| `parseCoordinationRequestMarker(text)` | `src/engine.js` | 1077 |
| `parseCoordinationResponseMarker(text)` | `src/engine.js` | 1115 |
| `applyCoordinationResponseAction(collab, parsed)` | `src/engine.js` | 1146 |
| `_buildRecipientMessage(displayName, mode, body)` | `src/engine.js` | 1197 |
| `_requesterDisplayName(requester)` | `src/engine.js` | 1211 |
| `applyCoordinationRequestAction(collab, parsed)` | `src/engine.js` | 1231 |
| COORD_HINT block (Sprint 16 F4) | `src/engine.js` | 2991–3024 |
| `buildSystemPrompt(collaborator, opts = {})` | `src/prompts/system.js` | 810 |
| `opts.coordHint → ctx.coordHint` | `src/prompts/system.js` | 813 |
| COORD_HINT async injection | `src/prompts/system.js` | 987–989 |
| COORD_HINT sync injection | `src/prompts/system.js` | 1020–1022 |
| Temporal grounding (`**Data/hora agora (BRT):**`) | `src/prompts/system.js` | 190 |
| Temporal grounding (`**Amanhã (BRT):**`) | `src/prompts/system.js` | 191 |

### Ponto de inserção de `buildActiveCoordinationContext`

A função deve ser inserida em `src/engine.js` imediatamente **antes** de `processMessage` (linha 2972). Inserir após a última função de coordination da Sprint 16.

### Ponto de wiring em processMessage

Linha 3025 atual:
```js
let { systemPrompt, ctx } = await buildSystemPrompt(collab, { lastUserMessage: text, coordHint });
```
A chamada a `buildActiveCoordinationContext` vai entre a linha 3024 (fim do bloco COORD_HINT) e a linha 3025 (buildSystemPrompt).

### Padrão COORD_HINT vs ACC (Decisão 5.3)

- **COORD_HINT** (Sprint 16): injetado apenas para o *recipient* de um request aberto. Instrui TOM a emitir `<<COORDINATION_RESPONSE>>` quando a mensagem atual parece uma resposta.
- **ACC** (Sprint 17): injetado para o *requester* (e qualquer collab com requests). Resolve pronomes, elipse, thread dominante. NÃO substitui COORD_HINT.

### Estado de `coordination_requests` relevante

Tabela Supabase: `coordination_requests`. Campos usados: `id`, `requester_id`, `recipient_id`, `mode`, `message_body`, `status` (`pending`, `sent`, `responded`), `created_at`, `responded_at`, `response_summary`. Joins com `collaborators` (campos: `id`, `full_name`, `first_name`, `phone`, `role`, `is_active`).

---

## File Structure

| Arquivo | Ação | Responsabilidade |
|---|---|---|
| `src/engine.js` | **Modificar** | Adicionar `buildActiveCoordinationContext` (Fatia 1) + wiring em `processMessage` (Fatia 2) + strip defense-in-depth em `applyCoordinationRequestAction` (Fatia 4) |
| `src/prompts/system.js` | **Modificar** | Receber `opts.coordContext`, injetar no async prompt (l.989) e sync prompt (l.1022), adicionar âncora semanal (l.192) |
| `skills/coordenacao-conversacional.md` | **Modificar** | Nova seção "Como consumir [ACTIVE_COORDINATION_CONTEXT]" ao final |

Zero arquivos novos. Zero migrations. Zero mudanças no dispatcher.

---

## Task 1 — Fatia 1: `buildActiveCoordinationContext` core (engine.js)

**Files:**
- Modify: `src/engine.js` (inserir função antes da linha 2972 `processMessage`)

### Objetivo
Função pura: 4 queries SQL paralelas → scoring de foco → retorna `{ block, focusCandidate, focusConfidence }`.

---

- [ ] **Step 1.1 — Verificar sintaxe do engine.js antes de qualquer mudança**

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output (sintaxe OK). Se falhar, parar e reportar.

---

- [ ] **Step 1.2 — Adicionar funções helper de formatação antes de `processMessage` (linha 2972)**

Inserir imediatamente antes da linha 2972 (`async function processMessage...`) no arquivo `D:/la-organizer/_remote/src/engine.js`:

```js
// ─────────────────────────────────────────────────────────────────────────────
// Sprint 17 — Active Coordination Context (ACC)
// ─────────────────────────────────────────────────────────────────────────────

/** Retorna os primeiros N chars com "…" se truncado. */
function _accTrunc(str, n) {
  if (!str) return '';
  return str.length > n ? str.slice(0, n) + '…' : str;
}

/** Retorna primeiros 8 chars de um UUID. */
function _accShort(id) {
  return id ? id.slice(0, 8) : '????????';
}

/** Minutos desde uma data ISO. */
function _accMinutesAgo(isoStr) {
  if (!isoStr) return null;
  return Math.floor((Date.now() - new Date(isoStr).getTime()) / 60000);
}

/** Primeiro nome a partir de full_name. */
function _accFirstName(fullName) {
  if (!fullName) return '?';
  return fullName.split(' ')[0];
}
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 1.3 — Adicionar Query Q1 como função isolada**

Imediatamente após as funções helper do Step 1.2, inserir:

```js
/** Q1 — último request criado pelo collab (últimos 7 dias, qualquer status). */
async function _accQ1(collabId) {
  const { data, error } = await supabase
    .from('coordination_requests')
    .select(`
      id, recipient_id, mode, message_body, status, created_at,
      recipient:collaborators!coordination_requests_recipient_id_fkey(full_name)
    `)
    .eq('requester_id', collabId)
    .gte('created_at', new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString())
    .order('created_at', { ascending: false })
    .limit(1)
    .maybeSingle();
  if (error) { console.warn('[ACC] Q1 error:', error.message); return null; }
  if (!data) return null;
  return { ...data, recipient_name: data.recipient?.full_name ?? null };
}
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 1.4 — Adicionar Query Q2**

```js
/** Q2 — último request onde collab é recipient (últimas 24h, status aberto). */
async function _accQ2(collabId) {
  const { data, error } = await supabase
    .from('coordination_requests')
    .select(`
      id, requester_id, mode, message_body, status, created_at,
      requester:collaborators!coordination_requests_requester_id_fkey(full_name)
    `)
    .eq('recipient_id', collabId)
    .in('status', ['pending', 'sent', 'responded'])
    .gte('created_at', new Date(Date.now() - 24 * 60 * 60 * 1000).toISOString())
    .order('created_at', { ascending: false })
    .limit(1)
    .maybeSingle();
  if (error) { console.warn('[ACC] Q2 error:', error.message); return null; }
  if (!data) return null;
  return { ...data, requester_name: data.requester?.full_name ?? null };
}
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 1.5 — Adicionar Query Q3**

```js
/** Q3 — última resposta recebida pelo collab como requester (últimos 7 dias). */
async function _accQ3(collabId) {
  const { data, error } = await supabase
    .from('coordination_requests')
    .select(`
      id, recipient_id, mode, response_summary, responded_at,
      responder:collaborators!coordination_requests_recipient_id_fkey(full_name)
    `)
    .eq('requester_id', collabId)
    .eq('status', 'responded')
    .gte('responded_at', new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString())
    .order('responded_at', { ascending: false })
    .limit(1)
    .maybeSingle();
  if (error) { console.warn('[ACC] Q3 error:', error.message); return null; }
  if (!data) return null;
  return { ...data, responder_name: data.responder?.full_name ?? null };
}
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 1.6 — Adicionar Query Q4 (LIMIT 5 — Decisão 5.2)**

```js
/** Q4 — requests abertos envolvendo collab em qualquer lado (últimas 48h, máx 5). */
async function _accQ4(collabId) {
  const cutoff = new Date(Date.now() - 48 * 60 * 60 * 1000).toISOString();
  const { data, error } = await supabase
    .from('coordination_requests')
    .select(`
      id, requester_id, recipient_id, mode, message_body, status, created_at,
      requester:collaborators!coordination_requests_requester_id_fkey(full_name),
      recipient:collaborators!coordination_requests_recipient_id_fkey(full_name)
    `)
    .or(`requester_id.eq.${collabId},recipient_id.eq.${collabId}`)
    .in('status', ['pending', 'sent'])
    .gte('created_at', cutoff)
    .order('created_at', { ascending: false })
    .limit(5);
  if (error) { console.warn('[ACC] Q4 error:', error.message); return []; }
  return (data || []).map(r => ({
    ...r,
    requester_name: r.requester?.full_name ?? null,
    recipient_name: r.recipient?.full_name ?? null,
  }));
}
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 1.7 — Adicionar função `_accScoreFocus(collabId, q1, q2, q3, q4)`**

```js
/**
 * Heurística de seleção de FOCUS_CANDIDATE.
 * Avalia em ordem de prioridade decrescente (spec §2.1.2).
 * Retorna { focusCandidate, focusConfidence }.
 */
function _accScoreFocus(collabId, q1, q2, q3, q4) {
  const now = Date.now();
  const min30 = 30 * 60 * 1000;
  const min120 = 120 * 60 * 1000;

  // P1: Q3 com resposta < 30min
  if (q3 && q3.responded_at) {
    const age = now - new Date(q3.responded_at).getTime();
    if (age < min30) {
      return {
        focusCandidate: {
          actorName: _accFirstName(q3.responder_name),
          requestId: q3.id,
          role: 'requester',
          reason: 'última resposta recebida',
        },
        focusConfidence: 'high',
      };
    }
  }

  // P2: Q1 com created_at < 30min e status='sent'
  if (q1 && q1.status === 'sent' && q1.created_at) {
    const age = now - new Date(q1.created_at).getTime();
    if (age < min30) {
      return {
        focusCandidate: {
          actorName: _accFirstName(q1.recipient_name),
          requestId: q1.id,
          role: 'requester',
          reason: 'request recém-criado',
        },
        focusConfidence: 'high',
      };
    }
  }

  // P3: Q4 com exatamente 1 request aberto
  if (q4 && q4.length === 1) {
    const r = q4[0];
    const otherName = r.requester_id === collabId ? r.recipient_name : r.requester_name;
    const role = r.requester_id === collabId ? 'requester' : 'recipient';
    return {
      focusCandidate: {
        actorName: _accFirstName(otherName),
        requestId: r.id,
        role,
        reason: 'único request aberto',
      },
      focusConfidence: 'high',
    };
  }

  // P4: Q4 com 2+ requests todos com o mesmo ator (clustering)
  if (q4 && q4.length > 1) {
    const actorIds = q4.map(r => r.requester_id === collabId ? r.recipient_id : r.requester_id);
    const allSame = actorIds.every(id => id === actorIds[0]);
    if (allSame) {
      const r = q4[0];
      const otherName = r.requester_id === collabId ? r.recipient_name : r.requester_name;
      const role = r.requester_id === collabId ? 'requester' : 'recipient';
      return {
        focusCandidate: {
          actorName: _accFirstName(otherName),
          requestId: r.id,
          role,
          reason: 'múltiplos requests com mesmo ator',
        },
        focusConfidence: 'medium',
      };
    }
  }

  // P5: Q3 com resposta entre 30-120min
  if (q3 && q3.responded_at) {
    const age = now - new Date(q3.responded_at).getTime();
    if (age >= min30 && age < min120) {
      return {
        focusCandidate: {
          actorName: _accFirstName(q3.responder_name),
          requestId: q3.id,
          role: 'requester',
          reason: 'resposta recente (> 30min)',
        },
        focusConfidence: 'medium',
      };
    }
  }

  // P6: Q4 com 2+ requests com atores distintos → low
  if (q4 && q4.length > 1) {
    return { focusCandidate: null, focusConfidence: 'low' };
  }

  // P7: tudo vazio
  return { focusCandidate: null, focusConfidence: 'none' };
}
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 1.8 — Adicionar função `_accBuildBlock(collabId, q1, q2, q3, q4, focusCandidate, focusConfidence)` com montagem e fallback para 3 itens**

```js
/**
 * Monta o bloco [ACTIVE_COORDINATION_CONTEXT].
 * Limite duro de 500 chars (spec §2.1.4 + Decisão 5.2).
 * Fallback: se block > 500 chars, reconstrói com max 3 requests abertos.
 */
function _accBuildBlock(collabId, q1, q2, q3, q4, focusCandidate, focusConfidence) {
  function buildLines(openRequests) {
    const lines = ['[ACTIVE_COORDINATION_CONTEXT]'];
    if (q1) {
      const min = _accMinutesAgo(q1.created_at);
      lines.push(`- Último request criado por você: ${_accShort(q1.id)} | recipient=${_accFirstName(q1.recipient_name)} | "${_accTrunc(q1.message_body, 60)}" | há ${min}min`);
    }
    if (q2) {
      const min = _accMinutesAgo(q2.created_at);
      lines.push(`- Último request onde você é recipient: ${_accShort(q2.id)} | from=${_accFirstName(q2.requester_name)} | "${_accTrunc(q2.message_body, 60)}" | há ${min}min`);
    }
    if (q3) {
      const min = _accMinutesAgo(q3.responded_at);
      lines.push(`- Última resposta recebida: ${_accShort(q3.id)} | de=${_accFirstName(q3.responder_name)} | "${_accTrunc(q3.response_summary, 60)}" | há ${min}min`);
    }
    if (openRequests && openRequests.length > 0) {
      lines.push('- Requests abertos:');
      for (const r of openRequests) {
        const other = r.requester_id === collabId ? _accFirstName(r.recipient_name) : _accFirstName(r.requester_name);
        lines.push(`  • ${_accShort(r.id)} ↔ ${other} | mode=${r.mode} | "${_accTrunc(r.message_body, 40)}"`);
      }
    }
    lines.push('');
    if (focusCandidate) {
      lines.push(`FOCUS_CANDIDATE: ${focusCandidate.actorName} (req ${_accShort(focusCandidate.requestId)}, você=${focusCandidate.role}, reason=${focusCandidate.reason})`);
      lines.push(`FOCUS_CONFIDENCE: ${focusConfidence}`);
    } else {
      lines.push(`FOCUS_CONFIDENCE: ${focusConfidence} — sem requests ativos`);
    }
    lines.push('');
    lines.push('Use isso para resolver pronomes/elipsis. Se confidence=low, pergunte citando candidatos pelo nome.');
    return lines.join('\n');
  }

  // Primeira tentativa: até 5 requests abertos
  let block = buildLines(q4 ? q4.slice(0, 5) : []);

  // Fallback: se > 500 chars, truncar para 3 requests abertos (Decisão 5.2)
  if (block.length > 500) {
    block = buildLines(q4 ? q4.slice(0, 3) : []);
  }

  return block;
}
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 1.9 — Adicionar a função principal `buildActiveCoordinationContext(collab)`**

```js
/**
 * Sprint 17 — Constrói o Active Coordination Context para o collab.
 * 4 queries paralelas + scoring + bloco de até 500 chars.
 * Retorna { block, focusCandidate, focusConfidence }.
 * block é null se collab não tem nenhum request relevante.
 */
async function buildActiveCoordinationContext(collab) {
  try {
    const [q1, q2, q3, q4] = await Promise.all([
      _accQ1(collab.id),
      _accQ2(collab.id),
      _accQ3(collab.id),
      _accQ4(collab.id),
    ]);

    // Se tudo vazio, retorna null block
    if (!q1 && !q2 && !q3 && (!q4 || q4.length === 0)) {
      return { block: null, focusCandidate: null, focusConfidence: 'none' };
    }

    const { focusCandidate, focusConfidence } = _accScoreFocus(collab.id, q1, q2, q3, q4);
    const block = _accBuildBlock(collab.id, q1, q2, q3, q4, focusCandidate, focusConfidence);

    return { block, focusCandidate, focusConfidence };
  } catch (err) {
    console.error('[ACC] buildActiveCoordinationContext error:', err.message);
    return { block: null, focusCandidate: null, focusConfidence: 'none' };
  }
}
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 1.10 — Smoke test SQL: verificar que coordination_requests tem dados para testar Q1-Q4**

Use o Supabase MCP (`mcp__4c04bb52-f946-4fe8-85f6-b01d200f8c20__execute_sql`) com project_id `cesnbnrynvxvgdhfmaua`:

```sql
-- Verifica estrutura e dados recentes (sem alterar nada)
SELECT
  COUNT(*) FILTER (WHERE status IN ('pending','sent')) AS abertos,
  COUNT(*) FILTER (WHERE status = 'responded') AS respondidos,
  MAX(created_at) AS mais_recente
FROM coordination_requests
WHERE created_at > now() - interval '7 days';
```

Esperado: sem erro. Valor de `abertos` e `respondidos` pode ser 0 (ambiente limpo) — OK, a função retorna null block nesse caso.

---

## Task 2 — Fatia 2: Wiring em `processMessage` + injeção em `system.js`

**Files:**
- Modify: `src/engine.js` (wiring após linha 3024, antes da chamada `buildSystemPrompt` na linha 3025)
- Modify: `src/prompts/system.js` (linha 813 para receber opts.coordContext; linha 989 para injeção async; linha 1022 para injeção sync)

---

- [ ] **Step 2.1 — Adicionar wiring de ACC em `processMessage` (engine.js linha 3025)**

Localizar em `D:/la-organizer/_remote/src/engine.js` a linha exata:
```js
  let { systemPrompt, ctx } = await buildSystemPrompt(collab, { lastUserMessage: text, coordHint });
```
(Linha 3025 — apenas esta linha, que é a chamada após o bloco COORD_HINT que termina na linha 3024.)

Substituir por:
```js
  // Sprint 17 — ACC: contexto ativo de coordenação (foco dominante + pronomes)
  // COORD_HINT (Sprint 16) permanece inalterado acima — COORD_HINT e ACC convivem (Decisão 5.3)
  let coordContext = null;
  {
    const acc = await buildActiveCoordinationContext(collab);
    if (acc.block) {
      coordContext = acc.block;
      console.log(`[ACC] focusConfidence=${acc.focusConfidence} focusCandidate=${acc.focusCandidate?.actorName ?? 'none'}`);
    }
  }

  let { systemPrompt, ctx } = await buildSystemPrompt(collab, { lastUserMessage: text, coordHint, coordContext });
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 2.2 — Em `system.js`, receber `opts.coordContext` na linha 813**

Localizar em `D:/la-organizer/_remote/src/prompts/system.js` exatamente:
```js
  if (opts.coordHint) ctx.coordHint = opts.coordHint;
```
(Linha 813.)

Substituir por:
```js
  if (opts.coordHint) ctx.coordHint = opts.coordHint;
  if (opts.coordContext) ctx.coordContext = opts.coordContext;   // Sprint 17 ACC
```

```bash
node -c D:/la-organizer/_remote/src/prompts/system.js
```
Esperado: sem output.

---

- [ ] **Step 2.3 — Injeção no prompt assíncrono (após linha 989 — após bloco COORD_HINT)**

Localizar em `D:/la-organizer/_remote/src/prompts/system.js` exatamente:
```js
  if (ctx && ctx.coordHint) {
    systemPrompt += '\n\n' + ctx.coordHint;
  }

  const totalTasks
```
(Linhas 987–991.)

Substituir por:
```js
  if (ctx && ctx.coordHint) {
    systemPrompt += '\n\n' + ctx.coordHint;
  }

  // Sprint 17 — ACC injection (contexto ativo de coordenação; convive com COORD_HINT — Decisão 5.3)
  if (ctx && ctx.coordContext) {
    systemPrompt += '\n\n' + ctx.coordContext;
  }

  const totalTasks
```

```bash
node -c D:/la-organizer/_remote/src/prompts/system.js
```
Esperado: sem output.

---

- [ ] **Step 2.4 — Injeção no prompt síncrono (após linha 1022 — após bloco COORD_HINT sync)**

Localizar em `D:/la-organizer/_remote/src/prompts/system.js` exatamente:
```js
  // Sprint 16 — COORD_HINT injection (só presente quando recipient tem recados abertos)
  if (ctx && ctx.coordHint) {
    syncPrompt += '\n\n' + ctx.coordHint;
  }
  return syncPrompt;
```
(Linhas 1019–1023.)

Substituir por:
```js
  // Sprint 16 — COORD_HINT injection (só presente quando recipient tem recados abertos)
  if (ctx && ctx.coordHint) {
    syncPrompt += '\n\n' + ctx.coordHint;
  }
  // Sprint 17 — ACC injection
  if (ctx && ctx.coordContext) {
    syncPrompt += '\n\n' + ctx.coordContext;
  }
  return syncPrompt;
```

```bash
node -c D:/la-organizer/_remote/src/prompts/system.js
```
Esperado: sem output.

---

- [ ] **Step 2.5 — Smoke test SQL: simular saída do ACC para um collab com requests abertos**

Use o Supabase MCP:

```sql
-- Verifica se há dados para simular Q4 (requests abertos últimas 48h)
SELECT
  cr.id,
  cr.requester_id,
  cr.recipient_id,
  cr.mode,
  cr.status,
  cr.created_at,
  req.full_name AS requester_name,
  rec.full_name AS recipient_name
FROM coordination_requests cr
LEFT JOIN collaborators req ON req.id = cr.requester_id
LEFT JOIN collaborators rec ON rec.id = cr.recipient_id
WHERE cr.status IN ('pending', 'sent')
  AND cr.created_at > now() - interval '48 hours'
ORDER BY cr.created_at DESC
LIMIT 5;
```

Esperado: 0 ou mais rows. Se 0, ACC retornará `block: null` — comportamento correto (não injeta nada).

---

## Task 3 — Fatia 3: Skill — nova seção "Como consumir [ACTIVE_COORDINATION_CONTEXT]"

**Files:**
- Modify: `skills/coordenacao-conversacional.md` (adicionar seção ao final, após `## Detecção de resposta`)

---

- [ ] **Step 3.1 — Localizar o final do arquivo para ponto de inserção**

```bash
wc -l D:/la-organizer/_remote/skills/coordenacao-conversacional.md
```
Verificar a última seção é `## Detecção de resposta` (e seu conteúdo sobre `<<COORDINATION_RESPONSE>>`).

---

- [ ] **Step 3.2 — Adicionar tabela de confirmação contextual complementar à seção "REGRA — Quando confirmar"**

Localizar em `D:/la-organizer/_remote/skills/coordenacao-conversacional.md` exatamente:
```
**Confirmação não é cuidado, é fricção desnecessária. Confie no relay_assisted.**
```

Substituir por:
```
**Confirmação não é cuidado, é fricção desnecessária. Confie no relay_assisted.**

#### Tabela complementar — ACC presente (Sprint 17)

| Situação | ACC presente | Ação |
|---|---|---|
| `relay_literal` sem texto entre aspas | qualquer | **Sempre perguntar** — pedir o texto verbatim |
| Destinatário ambíguo (2+ homônimos) | qualquer | **Sempre perguntar** — citar nomes completos |
| Request sem actor identificável no contexto | qualquer | **Sempre perguntar** |
| Modo ambíguo (relay vs followup) | qualquer | **Perguntar se ambíguo** |
| 2+ candidatos com confiança próxima | `confidence=low` | **Perguntar** citando candidatos pelo nome |
| Comando claro + ACC com `confidence=high` | `confidence=high` | **Não perguntar** — age direto |
| Relay com recipient explícito + objetivo claro | qualquer | **Não perguntar** — emite direto |
```

---

- [ ] **Step 3.3 — Adicionar nova seção "Como consumir [ACTIVE_COORDINATION_CONTEXT]" ao final do arquivo**

Adicionar ao final de `D:/la-organizer/_remote/skills/coordenacao-conversacional.md`:

```markdown
## Como consumir [ACTIVE_COORDINATION_CONTEXT]

Quando o system prompt contiver um bloco `[ACTIVE_COORDINATION_CONTEXT]`, use-o para
resolver referências implícitas antes de pedir confirmação.

> **ATENÇÃO — dois sinais distintos (Decisão 5.3):**
> - `[COORD_HINT]` → gatilho exclusivo para emissão de `<<COORDINATION_RESPONSE>>` quando você (como recipient) recebe uma mensagem que parece resposta a um recado aberto. Sprint 16.
> - `[ACTIVE_COORDINATION_CONTEXT]` → resolve pronomes, elipse e thread dominante para qualquer collab. Sprint 17. Os dois convivem no system prompt e têm funções diferentes. NÃO confundir.

### Tabela de resolução por tipo de frase

| Frase do usuário | Como resolver |
|---|---|
| "agradece a ele/ela por mim" | Actor = `FOCUS_CANDIDATE` (último de quem recebeu resposta em Q3) |
| "manda" / "confirma" / "autorizado" / "responde" sem objeto explícito | Request = mais recente em Q1 ou Q4 com status aberto |
| "ele/ela/esse/aquele" sem antecedente claro na conversa | → `FOCUS_CANDIDATE.actorName` |
| Pronome + 2+ candidatos com confiança similar (`confidence=low`) | PERGUNTAR: "Pra [nome1] ou pro [nome2]?" |

### Política por nível de confiança

**`FOCUS_CONFIDENCE: high`** — age diretamente, sem confirmar.
- Input: "Agradece a ele!" (após TOM exibir "Rafinha respondeu: vai verificar amanhã cedo.")
- Output: TOM envia mensagem de agradecimento ao Rafinha sem perguntar.

**`FOCUS_CONFIDENCE: medium`** — resolve, mas inclui microconfirmação na resposta (Decisão 5.1).
- Input: "Diz que está autorizado." (request aberto com Rafinha, criado há 45min)
- Output: "Vou avisar o Rafinha que está autorizado — pode mandar?"

**`FOCUS_CONFIDENCE: low`** — pergunta citando candidatos pelo nome.
- Input: "Diz que está autorizado." (requests abertos com Anne E Rafinha, ambos recentes)
- Output: "Pra Anne ou pro Rafinha?"

**`FOCUS_CONFIDENCE: none`** — sem ACC ativo; segue fluxo padrão de coordenação. `[COORD_HINT]` ainda funciona normalmente para detecção de resposta.

### Exemplos concretos por cenário

**Cenário A — Agradecimento anafórico (confidence=high, age sem confirmar):**
TOM: "Rafinha respondeu que vai verificar amanhã cedo."
Alf: "Agradece a ele por mim."
→ ACC: `FOCUS_CANDIDATE: Rafinha (req ab12cd34, você=requester, reason=última resposta recebida)`, `FOCUS_CONFIDENCE: high`
→ TOM envia "O Alf manda agradecer!" ao Rafinha sem confirmar.

**Cenário B — Elipse com múltiplos abertos (confidence=low, pergunta):**
ACC: requests abertos com Anne (45min atrás) e Rafinha (1h atrás). `FOCUS_CONFIDENCE: low`
Alf: "Diz que está autorizado."
→ TOM: "Pra Anne ou pro Rafinha?"

**Cenário C — Comando claro sem ACC necessário (emite direto):**
Alf: "fala com Anne sobre o briefing de amanhã"
→ Recipient explícito, objetivo claro → emita relay_assisted diretamente. A REGRA "quando confirmar antes de emitir" ainda se aplica normalmente.

**Cenário D — Loop de mediação, fechamento preciso (confidence=high, usa request_id do ACC):**
ACC: `FOCUS_CANDIDATE: Anne (req ef56ab78, você=requester, reason=único request aberto)`, `FOCUS_CONFIDENCE: high`
Anne responde ao TOM via WhatsApp.
→ Você (como TOM processando a mensagem da Anne) já tem COORD_HINT com o request_id exato.
→ Emite `<<COORDINATION_RESPONSE>>` com `request_id` do COORD_HINT (não do ACC — são sinais diferentes).

**Cenário E — Microconfirmação medium (confidence=medium, inclui frase de confirmação):**
ACC: `FOCUS_CANDIDATE: Rafinha (req cd78ef90, você=requester, reason=múltiplos requests com mesmo ator)`, `FOCUS_CONFIDENCE: medium`
Alf: "manda"
→ TOM: "Vou avisar o Rafinha — pode mandar?"
```

---

- [ ] **Step 3.4 — Smoke test: verificar que o arquivo tem todas as seções esperadas**

```bash
grep -n "^## \|^### " D:/la-organizer/_remote/skills/coordenacao-conversacional.md
```

Esperado: deve incluir a nova seção `## Como consumir [ACTIVE_COORDINATION_CONTEXT]` e as subseções `### Tabela de resolução`, `### Política por nível de confiança`, `### Exemplos concretos`.

---

## Task 4 — Fatia 4: Defense-in-depth + reforço temporal

**Files:**
- Modify: `src/engine.js` (strip patterns antes da linha 1325 em `applyCoordinationRequestAction`)
- Modify: `src/prompts/system.js` (âncora semanal após linha 191)

---

- [ ] **Step 4.1 — Adicionar strip de cabeçalho duplicado em `applyCoordinationRequestAction` (engine.js)**

Localizar em `D:/la-organizer/_remote/src/engine.js` exatamente as linhas 1324–1326:
```js
  // 7. Enviar WhatsApp ao recipient (UX §6)
  const requesterDisplayName = _requesterDisplayName(collab);
  const recipientMsg = _buildRecipientMessage(requesterDisplayName, parsed.mode, parsed.message_body);
```

Substituir por:
```js
  // 7. Enviar WhatsApp ao recipient (UX §6)

  // Sprint 17 — defense-in-depth: strip de prefixo de origem no message_body.
  // Garante que o engine não dobre o cabeçalho mesmo que a skill falhe na REGRA CRÍTICA.
  const STRIP_HEADER_PATTERNS = [
    /^\s*o\s+[\wÀ-ú]+(?:\s+[\wÀ-ú]+)?\s*(?:\([^)]{0,40}\))?\s*(?:pediu|me pediu|disse|mandou|pediu pra mim).{0,40}:\s*/i,
    /^\s*alf(?:redo)?\s+pediu.{0,40}:\s*/i,
    /^\s*(?:o\s+)?requester\s+pediu.{0,40}:\s*/i,
  ];
  let _sanitizedBody = parsed.message_body;
  let _stripped = false;
  for (const pattern of STRIP_HEADER_PATTERNS) {
    const _before = _sanitizedBody;
    _sanitizedBody = _sanitizedBody.replace(pattern, '').trim();
    if (_sanitizedBody !== _before) {
      _stripped = true;
      console.warn(`[CoordinationRequest] strip cabeçalho duplicado detectado em req ${inserted?.id?.slice(0, 8) ?? 'unknown'}`);
    }
  }
  // Se strip ocorreu mas body ficou vazio (edge case: body era só o cabeçalho), preservar original
  if (_stripped && _sanitizedBody.length === 0) {
    console.error(`[CoordinationRequest] strip resultou em body vazio — mantendo original`);
    _stripped = false;
  }
  const finalBody = _stripped ? _sanitizedBody : parsed.message_body;

  const requesterDisplayName = _requesterDisplayName(collab);
  const recipientMsg = _buildRecipientMessage(requesterDisplayName, parsed.mode, finalBody);
```

```bash
node -c D:/la-organizer/_remote/src/engine.js
```
Esperado: sem output.

---

- [ ] **Step 4.2 — Adicionar âncora semanal em `system.js` após linha 191**

Localizar em `D:/la-organizer/_remote/src/prompts/system.js` exatamente:
```js
  lines.push(`**Amanhã (BRT):** ${tomorrowISO} (${tomorrowWD})`);
```
(Linha 191.)

Substituir por:
```js
  lines.push(`**Amanhã (BRT):** ${tomorrowISO} (${tomorrowWD})`);

  // Sprint 17 — âncora semanal explícita (spec §2.6)
  {
    const todayDate = new Date(todayISO + 'T15:00:00.000Z'); // meio-dia BRT
    const todayDOW = todayDate.getUTCDay(); // 0=dom, 1=seg, ..., 6=sáb
    const diffToMonday = (todayDOW === 0 ? -6 : 1 - todayDOW);
    const monday = new Date(todayDate);
    monday.setUTCDate(monday.getUTCDate() + diffToMonday);
    const _fmtDate = (d) => `${String(d.getUTCDate()).padStart(2, '0')}/${String(d.getUTCMonth() + 1).padStart(2, '0')}`;
    const _weekDays = ['dom', 'seg', 'ter', 'qua', 'qui', 'sex', 'sáb'];
    const weekDates = Array.from({ length: 7 }, (_, i) => {
      const d = new Date(monday);
      d.setUTCDate(d.getUTCDate() + i);
      return `${_weekDays[d.getUTCDay()]} ${_fmtDate(d)}`;
    });
    lines.push(`**Esta semana (BRT):** ${weekDates.join(' · ')}`);
  }
```

```bash
node -c D:/la-organizer/_remote/src/prompts/system.js
```
Esperado: sem output.

---

- [ ] **Step 4.3 — Smoke test: verificar output do temporal grounding com data de hoje**

Criar script temporário `D:/la-organizer/_remote/test-temporal.js`:

```js
const todayISO = '2026-05-03';
const todayDate = new Date(todayISO + 'T15:00:00.000Z');
const todayDOW = todayDate.getUTCDay();
const diffToMonday = (todayDOW === 0 ? -6 : 1 - todayDOW);
const monday = new Date(todayDate);
monday.setUTCDate(monday.getUTCDate() + diffToMonday);
const fmtDate = (d) => `${String(d.getUTCDate()).padStart(2, '0')}/${String(d.getUTCMonth() + 1).padStart(2, '0')}`;
const weekDays = ['dom', 'seg', 'ter', 'qua', 'qui', 'sex', 'sáb'];
const weekDates = Array.from({ length: 7 }, (_, i) => {
  const d = new Date(monday);
  d.setUTCDate(d.getUTCDate() + i);
  return `${weekDays[d.getUTCDay()]} ${fmtDate(d)}`;
});
console.log(`Esta semana (BRT): ${weekDates.join(' · ')}`);
```

```bash
node D:/la-organizer/_remote/test-temporal.js
```

Esperado: `Esta semana (BRT): seg 04/05 · ter 05/05 · qua 06/05 · qui 07/05 · sex 08/05 · sáb 09/05 · dom 10/05`

```bash
rm D:/la-organizer/_remote/test-temporal.js
```

---

- [ ] **Step 4.4 — Smoke test strip: verificar padrão regex não falha em body limpo**

Criar script temporário `D:/la-organizer/_remote/test-strip.js`:

```js
const STRIP_HEADER_PATTERNS = [
  /^\s*o\s+[\wÀ-ú]+(?:\s+[\wÀ-ú]+)?\s*(?:\([^)]{0,40}\))?\s*(?:pediu|me pediu|disse|mandou|pediu pra mim).{0,40}:\s*/i,
  /^\s*alf(?:redo)?\s+pediu.{0,40}:\s*/i,
  /^\s*(?:o\s+)?requester\s+pediu.{0,40}:\s*/i,
];

const cases = [
  // Deve ser stripped
  { input: 'O Alf pediu pra te avisar: vai ter reunião amanhã', expect: 'vai ter reunião amanhã' },
  { input: 'Alfredo pediu que você confirmasse: presente na sexta?', expect: 'presente na sexta?' },
  // Deve ser preservado (body limpo)
  { input: 'vai ter reunião amanhã', expect: 'vai ter reunião amanhã' },
  { input: 'Confirmar presença na sexta?', expect: 'Confirmar presença na sexta?' },
];

let failed = 0;
for (const c of cases) {
  let result = c.input;
  for (const p of STRIP_HEADER_PATTERNS) { result = result.replace(p, '').trim(); }
  const ok = result === c.expect;
  console.log(ok ? '✓' : '✗', JSON.stringify(c.input), '->', JSON.stringify(result), ok ? '' : `(expected: ${JSON.stringify(c.expect)})`);
  if (!ok) failed++;
}
process.exit(failed > 0 ? 1 : 0);
```

```bash
node D:/la-organizer/_remote/test-strip.js
```

Esperado: todas as linhas com `✓`. Exit code 0.

```bash
rm D:/la-organizer/_remote/test-strip.js
```

---

## Task 5 — Fatia 5: Single deploy + validação E2E

**Files:**
- Deploy: `src/engine.js`, `src/prompts/system.js`, `skills/coordenacao-conversacional.md`

> Todas as Fatias 1-4 ficam locais. Esta tarefa faz o bundle commit+push+VPS.

---

- [ ] **Step 5.1 — Verificação final de sintaxe dos 3 arquivos**

```bash
node -c D:/la-organizer/_remote/src/engine.js
node -c D:/la-organizer/_remote/src/prompts/system.js
```

E verificar que o arquivo skill existe:
```bash
ls -la D:/la-organizer/_remote/skills/coordenacao-conversacional.md
```

Todos devem passar sem erro.

---

- [ ] **Step 5.2 — Clone temp, copy files, commit, push origin main**

```bash
cd /tmp
git clone <repo-url-do-main> sprint17-acc-deploy
cp D:/la-organizer/_remote/src/engine.js sprint17-acc-deploy/src/engine.js
cp D:/la-organizer/_remote/src/prompts/system.js sprint17-acc-deploy/src/prompts/system.js
cp D:/la-organizer/_remote/skills/coordenacao-conversacional.md sprint17-acc-deploy/skills/coordenacao-conversacional.md
cd sprint17-acc-deploy
git add src/engine.js src/prompts/system.js skills/coordenacao-conversacional.md
git commit -m "feat(sprint17): ACC — buildActiveCoordinationContext + wiring + skill + defense-in-depth + temporal"
git push origin main
```

Esperado: commit e push sem erro.

---

- [ ] **Step 5.3 — Deploy no VPS e restart do TOM**

```bash
ssh tom "cd /opt/LA-Organizer && git pull && pm2 restart tom"
```

Esperado: `git pull` mostra os 3 arquivos atualizados; `pm2 restart tom` confirma restart.

---

- [ ] **Step 5.4 — Cleanup clone temp**

```bash
cd /tmp && rm -rf sprint17-acc-deploy
```

---

- [ ] **Step 5.5 — Smoke test SQL pós-deploy: verificar que sistema está operacional**

Use o Supabase MCP:

```sql
-- Verifica que coordination_requests é acessível e tem a estrutura esperada
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'coordination_requests'
  AND column_name IN ('id','requester_id','recipient_id','mode','message_body',
                      'status','created_at','responded_at','response_summary')
ORDER BY column_name;
```

Esperado: 9 colunas retornadas sem erro.

---

- [ ] **Step 5.6 — E2E Cenário E1: Agradecimento anafórico (confidence=high, age sem confirmar)**

**Setup SQL** — criar request respondido há < 30 minutos:
```sql
INSERT INTO coordination_requests (
  requester_id, recipient_id, mode, message_body, status,
  expects_response, response_summary, responded_at, sent_at
)
SELECT
  (SELECT id FROM collaborators WHERE full_name ILIKE '%alf%' LIMIT 1),
  (SELECT id FROM collaborators WHERE full_name ILIKE '%rafael%' OR full_name ILIKE '%rafinha%' LIMIT 1),
  'followup',
  'Você pode verificar o teclado da sala 3?',
  'responded',
  true,
  'Vou verificar amanhã cedo.',
  now() - interval '5 minutes',
  now() - interval '10 minutes'
RETURNING id, status, responded_at;
```

**Teste no WhatsApp:** enviar como Alf: `"Agradece a ele por mim"`

**Resultado esperado:**
- Log no PM2: `[ACC] focusConfidence=high focusCandidate=Rafinha`
- TOM envia mensagem de agradecimento ao Rafinha **sem fazer pergunta**

**Cleanup:**
```sql
DELETE FROM coordination_requests
WHERE message_body = 'Você pode verificar o teclado da sala 3?'
  AND response_summary = 'Vou verificar amanhã cedo.';
```

---

- [ ] **Step 5.7 — E2E Cenário E2: Elipse com 2 abertos (confidence=low, pergunta)**

**Setup SQL** — criar 2 requests abertos com atores diferentes:
```sql
-- Request 1: com Anne
INSERT INTO coordination_requests (requester_id, recipient_id, mode, message_body, status, sent_at)
SELECT
  (SELECT id FROM collaborators WHERE full_name ILIKE '%alf%' LIMIT 1),
  (SELECT id FROM collaborators WHERE full_name ILIKE '%anne%' LIMIT 1),
  'relay_assisted', 'Presença confirmada no Recreio amanhã.', 'sent', now() - interval '45 minutes'
RETURNING id;

-- Request 2: com Rafinha
INSERT INTO coordination_requests (requester_id, recipient_id, mode, message_body, status, sent_at)
SELECT
  (SELECT id FROM collaborators WHERE full_name ILIKE '%alf%' LIMIT 1),
  (SELECT id FROM collaborators WHERE full_name ILIKE '%rafael%' OR full_name ILIKE '%rafinha%' LIMIT 1),
  'relay_assisted', 'Material de estágio aprovado.', 'sent', now() - interval '60 minutes'
RETURNING id;
```

**Teste no WhatsApp:** enviar como Alf: `"Diz que está autorizado"`

**Resultado esperado:**
- Log: `[ACC] focusConfidence=low focusCandidate=none`
- TOM responde: "Pra Anne ou pro Rafinha?" (ou variação citando os dois nomes)

**Cleanup:**
```sql
DELETE FROM coordination_requests WHERE message_body IN (
  'Presença confirmada no Recreio amanhã.',
  'Material de estágio aprovado.'
);
```

---

- [ ] **Step 5.8 — E2E Cenário E4: Microconfirmação medium (confidence=medium)**

**Setup SQL** — request aberto há 45min, único ator, clustering:
```sql
INSERT INTO coordination_requests (requester_id, recipient_id, mode, message_body, status, sent_at)
SELECT
  (SELECT id FROM collaborators WHERE full_name ILIKE '%alf%' LIMIT 1),
  (SELECT id FROM collaborators WHERE full_name ILIKE '%rafael%' OR full_name ILIKE '%rafinha%' LIMIT 1),
  'relay_assisted', 'Criativos para o evento de sexta precisam de ajuste.', 'sent',
  now() - interval '45 minutes'
RETURNING id;

-- Segundo request com mesmo ator para forçar clustering (P4)
INSERT INTO coordination_requests (requester_id, recipient_id, mode, message_body, status, sent_at)
SELECT
  (SELECT id FROM collaborators WHERE full_name ILIKE '%alf%' LIMIT 1),
  (SELECT id FROM collaborators WHERE full_name ILIKE '%rafael%' OR full_name ILIKE '%rafinha%' LIMIT 1),
  'relay_assisted', 'Confirmar horário de entrada na sexta?', 'sent',
  now() - interval '50 minutes'
RETURNING id;
```

**Teste no WhatsApp:** enviar como Alf: `"manda"`

**Resultado esperado:**
- Log: `[ACC] focusConfidence=medium focusCandidate=Rafinha`
- TOM responde com microconfirmação: "Vou avisar o Rafinha — pode mandar?" (ou variação com "pode mandar?")

**Cleanup:**
```sql
DELETE FROM coordination_requests WHERE message_body IN (
  'Criativos para o evento de sexta precisam de ajuste.',
  'Confirmar horário de entrada na sexta?'
);
```

---

- [ ] **Step 5.9 — Non-regression Sprint 16, caso 1: relay_assisted com recipient explícito**

**Teste no WhatsApp:** enviar como director/coordinator: `"avisa a Anne que amanhã vou estar no Recreio"`

**Resultado esperado:**
- TOM confirma envio para Anne
- Anne recebe WhatsApp com cabeçalho correto (ex: "O Alf me pediu pra te avisar: ...")
- `message_body` na tabela NÃO contém o cabeçalho (strip defense-in-depth OK se skill falhou, mas preferência é skill já entrega corpo limpo)

**Verificação SQL:**
```sql
SELECT id, message_body, status
FROM coordination_requests
WHERE message_body ILIKE '%recreio%'
ORDER BY created_at DESC LIMIT 1;
```

---

- [ ] **Step 5.10 — Non-regression Sprint 16, caso 2: collaborator followup recusado**

**Teste no WhatsApp:** enviar como collaborator: `"cobra o Rafinha sobre os criativos"`

**Resultado esperado:**
- TOM recusa com texto: "Esse tipo de cobrança precisa vir do coordenador ou diretor..."
- Row em `coordination_requests` com `status='rejected_by_tom'` e `cancelled_reason='role_insufficient'`

**Verificação SQL:**
```sql
SELECT id, status, cancelled_reason
FROM coordination_requests
WHERE cancelled_reason = 'role_insufficient'
ORDER BY created_at DESC LIMIT 1;
```

---

## ⚠️ Atenções obrigatórias durante validação E2E (Alf 2026-05-03)

Estes 2 pontos exigem **olhar humano direto sobre o output real** durante a Fatia 5:

### A1 — Strip de cabeçalho duplicado (Task 4 Step 4.1)
A regex de strip resolve a duplicação observada em produção, mas regex desse tipo **pode morder texto legítimo se cresc er demais**. Validação obrigatória:
- Observar todas as mensagens enviadas a recipients durante F5 — se algum cabeçalho legítimo for cortado, voltar e ajustar pattern.
- Logar TODA execução de strip que efetivamente remove conteúdo (não apenas trim) — facilita auditoria pós-deploy.
- Considerar revisar a regex 1 semana depois com sample real de output em produção.

### A2 — Confidence=medium microconfirmação (Decisão 5.1)
A microconfirmação resolve o risco de ação errada silenciosa, mas pode virar fricção se for emitida em excesso. Validação obrigatória:
- Em F5, contar quantas microconfirmações foram emitidas vs quantas resoluções diretas (high) e perguntas explícitas (low).
- Se microconfirmação >50% dos turnos com ACC ativo, threshold de 30min para `high` está restrito demais → revisar para 45-60min.
- Inversamente, se microconfirmação <5%, threshold pode estar permissivo demais → revisar.

Estes pontos não bloqueiam execução das fatias 1-4. Aplicam-se durante e após F5, com retrospectiva 1 semana após deploy.

---

## Self-Review

### 1. Spec Coverage

| Seção do spec | Task que implementa | Status |
|---|---|---|
| §2.1 `buildActiveCoordinationContext` core | Task 1 (Steps 1.2–1.9) | ✓ |
| §2.1.1 Queries Q1–Q4 com LIMIT 5 (Decisão 5.2) | Task 1 Steps 1.3–1.6 | ✓ |
| §2.1.2 FOCUS_CANDIDATE heurística P1–P7 | Task 1 Step 1.7 | ✓ |
| §2.1.3 FOCUS_CONFIDENCE tabela | Task 1 Step 1.7 | ✓ |
| §2.1.4 Bloco 500 chars + fallback para 3 (Decisão 5.2) | Task 1 Step 1.8 | ✓ |
| §2.2 Wiring em processMessage | Task 2 Step 2.1 | ✓ |
| §2.3 Injeção em system.js (async + sync) | Task 2 Steps 2.2–2.4 | ✓ |
| §2.4 Skill: tabela resolução + política confiança + exemplos A–E | Task 3 Steps 3.2–3.3 | ✓ |
| §2.5 Defense-in-depth strip em applyCoordinationRequestAction | Task 4 Step 4.1 | ✓ |
| §2.6 Reforço temporal "Esta semana" em system.js | Task 4 Step 4.2 | ✓ |
| §2.7 Tabela confirmação contextual na skill | Task 3 Step 3.2 | ✓ |
| §5.1 confidence=medium → microconfirmar | Task 3 Step 3.3 (skill) | ✓ |
| §5.2 LIMIT 5 + fallback 3 | Task 1 Steps 1.6, 1.8 | ✓ |
| §5.3 COORD_HINT e ACC convivem (NÃO consolidar) | Task 2 Step 2.1 + Task 3 Step 3.3 | ✓ |
| Fatia 5 E2E Cenários E1–E5 | Task 5 Steps 5.6–5.10 | ✓ (E3 é Cenário C — command claro — cobert via E2E manual; E5 = non-regression caso 3) |
| Non-regression Sprint 16 (4 casos) | Task 5 Steps 5.9–5.10 | ✓ (2 automáticos; 2 implícitos em E2E) |

**Gap identificado:** O spec menciona Cenário E5 ("Loop fechamento preciso — Anne responde, TOM emite COORDINATION_RESPONSE com request_id correto"). Este cenário depende do recipient (Anne) enviar uma mensagem, o que é E2E puro no WhatsApp real. O plano cobre via non-regression (sprint 16 caso 3) e instrução explícita na skill (Step 3.3 Cenário D). Não há gap funcional — gap é apenas que E5 não tem fixture SQL.

### 2. Placeholder Scan

Nenhum "TBD", "TODO", "implement later" ou "fill in details" encontrado. Cada step muda código ou SQL concreto.

### 3. Type Consistency

- `buildActiveCoordinationContext(collab)` recebe `collab` com campo `collab.id` (string UUID) — consistente com uso em `processMessage` (linha 2984: `collab.id` usado em COORD_HINT).
- Retorna `{ block: string|null, focusCandidate: object|null, focusConfidence: string }` — consumido em Task 2 Step 2.1 via `acc.block`, `acc.focusConfidence`, `acc.focusCandidate?.actorName`.
- `_accScoreFocus` retorna `{ focusCandidate, focusConfidence }` — consumido por `_accBuildBlock` e `buildActiveCoordinationContext`. ✓
- `_accBuildBlock` usa `collabId` (string) para comparar com `r.requester_id` — ambos são UUIDs do Supabase. ✓
- `opts.coordContext` em `buildSystemPrompt`: recebido em linha 813 (Task 2.2), usado nas injeções async (Task 2.3) e sync (Task 2.4) via `ctx.coordContext`. ✓
- Strip: `finalBody` substituiu `parsed.message_body` apenas no `_buildRecipientMessage` call — a inserção no Supabase (antes da linha 1324) usa `parsed.message_body` original, sem alterar o que vai pra DB. ✓
