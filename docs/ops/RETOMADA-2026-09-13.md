# RETOMADA — 13/09/2026 (auditoria cruzada do ciclo de governança)

> Documento de handoff. Quem chegar aqui (sessão nova, contexto compactado, outro agente) deve
> conseguir retomar sem reabrir investigação. Fatos com prova; o que é opinião está marcado.

## 1. Estado em uma linha

**Acervo de achados: ZERO abertos** (288 corrigidos · 77 resolvidos · 49 descartados · 40 falso-positivo).
TOM no ar, árvore limpa, suíte **4904/4904 fail 0**, health check **25/25 sem aviso**.

## 2. O que foi feito hoje (3 consertos meus, todos com prova e no ar)

| SHA | KI | O que era | Prova |
|---|---|---|---|
| `277b2549` | MEMORIA-APAGAR-PELO-CHAT | Anne (12/09) pediu "pode tirar da lista"; o TOM respondeu "Tiro isso agora" e **não existia executor** — `persistMemoryRows` ignorava `action` e a chave `fact` não era aceita (schema_invalid) | 10 testes · 7 mutantes · E2E com o marcador real · memória `2efa1573` desativada |
| `2067bd6d` | TOTAL-DA-LISTA-E-RESPOSTA | Rose (04/07) pediu o total 2× e recebeu a lista de novo sem número; fecha `cfdf9bdb` e `ded67aeb` | 7 testes (fixtures = as 2 listas reais, somas batem: R$ 9.558,16 e R$ 9.914,32) · 6 mutantes · E2E 4/4 |
| `345e72d2` | MOTIVO-MARKER-CORTADO-EM-120 | O motivo do marcador cortava em 120 e apagou a mensagem do erro do LA Report (12/09 15:00) | 3 testes · 3 mutantes · orçamentos dos harness atualizados (sobra 27 → 207) |

**Decisões de produto do Alf (13/09):**
- **Não** capturar toda instrução de grupo. O contexto de grupo já é salvo sozinho — confirmado: 89 memórias de grupo, 87 ativas, nos 6 grupos; **44 entraram sem passar por aprovação**, e só as 11 que declaram mudança de comportamento vão pra fila do ok dele.
- Achado do Jereh (instrução no grupo ignorada, janela fechada) **fica como falso positivo** — é o desenho.
- Agendamento do dia 25/09 (medir "total da lista") **cancelado**: o conserto determinístico entrou no lugar.

## 3. Veredito sobre o ciclo do agente de governança (12 e 13/09)

Conferido com prova própria, **os números dele batem**:
- suíte 4884/4884 ✔ · carimbo 19/506 = 3,75% ✔ · plano de medição inexequível ✔ · acervo 4→2 ✔
- `f1bfb473` (áudio tratado como figurinha) é bug real, mas **não muda o caso do Jereh** (janela fechada) — ele registrou isso certo na nota; só a mensagem do grupo deu a entender o contrário.

**Erro que era meu, corrigido:** as "3 falhas de baseline" da suíte não existem. `system-loadout.test.js`
exige `TEST_COLLAB_ID`; sem a variável ele quebra no hook.
→ **Rodar sempre:** `TEST_COLLAB_ID=<uuid real> node --env-file=.env --test src/`

## 4. O que segue aberto (nada bloqueia, mas não esquecer)

1. **Resposta do TOM omite o dia** — Yuri (12/09) pediu tarefa "pra segunda"; a tarefa nasceu certa (14/09), mas a fala foi só "Anotado. Lembro às 14h". Baixo risco, não consertado.
2. **Alarmes de vitalidade (delegação e reagendamento)** — o agente diz que vão acusar por ~2 semanas sem serem defeito (as perguntas medidas são anteriores ao parser/conserto). **Não conferi por conta própria.** Se voltarem depois de ~27/09, aí é sinal.
3. **LA Report** — a falha de 12/09 15:00 foi passageira (testado hoje: 340 linhas, sem erro). Se repetir, agora o motivo do marcador traz a mensagem inteira.
4. **Rajada de cobranças** — Vitoria levou 6 mensagens em 13s (16:00, 12/09). **Não é defeito** (são 6 tarefas distintas), mas agrupar seria melhor UX. Decisão do Alf.

## 5. Protocolo da casa (o que NÃO pode ser esquecido)

- Cada conserto: teste vermelho → código → verde → **mutantes** (o arquivo sempre volta) → E2E pelo
  `processMessage` real no QA (`5bf93a06-…`, WhatsApp e voz interceptados, **nunca envio real**) →
  suíte completa → `git add` só dos arquivos → commit com Co-Authored-By → `pull --rebase` → `push` →
  `pm2 restart tom` só com árvore limpa (ignorar `_orphans_20260907/`) → KI + fechar achado.
- **Nunca** imprimir valor de env/segredo · **nunca** `git add -A` · sem telefone em log · `soul/` e
  `skills/` intocáveis · apagar dado de produção exige OK explícito.
- Transporte pra VPS por base64 (**barra invertida some no ssh**). Migrations vivem no Supabase
  (`supabase/migrations/` é gitignored).
- Mutante que sobrevive costuma ser trabalho de verdade, não ruído: hoje um deles evitou que
  "briefing" sozinho apagasse a anotação errada.

## 6. Onde olhar primeiro numa retomada

1. `tom_audit_findings` (status `novo`/`confirmado`) — hoje: vazio.
2. `tom_known_issues` pelos códigos citados aqui.
3. Memória do projeto: `project_triagem_1109_tarde_frentes.md` (checklist das frentes 1–7, todas fechadas).
4. Auditoria diária e ciclo de governança no grupo **LA ORGANIZER - TOM** (07:30 e ~08:30).
