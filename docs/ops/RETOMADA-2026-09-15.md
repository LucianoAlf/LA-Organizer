# Retomada — 15/09/2026 (auditoria cruzada do TOM)

Continua `RETOMADA-2026-09-13.md` (protocolo, catracas e armadilhas seguem valendo).

## Consertos no ar hoje

| SHA | Código | O que mudou |
|---|---|---|
| 99fc1d58 | aviso de duplicata só no mesmo horário | `src/lib/mesmo-horario.js`: o aviso "compromisso parecido" só aparece quando o horário se sobrepõe (sem fim = 1h). A mensagem virou "Você já tem um compromisso nesse mesmo horário (qui 17/09, 9h–10h)". O "2 — cria mesmo assim" grava `EVENT_CREATE executed` (fecha o alarme do 41cd063f). Caso do Alf: mentoria quinta × sexta. |
| 0711200d | nota honesta sem mentir a causa | `promise-honesty`: "não consigo por aqui" (sem marcador) ≠ "falhou" (marcador tentado). O veto é por frase, a oferta "é só me dizer" não vira promessa e "vou parar de cobrar" entrou no vocabulário. |
| d7210438 + c4dc4341 | PROJETO-SEM-COMO-PAUSAR-COBRANCA | `<<PROJECT_UPDATE>> pausar_cobranca`, colunas `projects.cobranca_pausada_ate/cobranca_pausa_motivo`, e o balanço pula o projeto pausado. O c4dc4341 pôs payload no rejected, depois que a catraca de rejeição cega ficou vermelha no d7210438. |
| a1fe84c6 | CARDAPIO-SEM-EXECUTOR | O cardápio "1 Resolvo agora · 2 Agendo · 3 Delego · 4 Preciso de apoio" ganhou executor antes do LLM (número, emoji ou palavras; só com cardápio em aberto). A notificação sai por `sendAndLink`. "1" = prazo hoje (status segue `pending`); "4" avisa quem criou. |

Suíte: **4972/4972**. Achados: e1ad051e (Juliana) e 85a251a4 (Rafinha) corrigidos; 41cd063f segue falso positivo, com nota.

## Regra nova do protocolo

Commit **só** com `grep -q "^# fail 0$"` no arquivo da suíte. Encadear com `&&` depois de um `grep` de resumo deixa passar uma suíte vermelha (foi o que aconteceu no d7210438).

## Em aberto

- **LA Report — timeout da `get_situacao_alunos_v1`** (outro projeto): 6 quedas desde 05/09. O teto é 8 s (`authenticator`); medido a 0,9–4,9 s, máxima de 5,8 s no pg_stat_statements. O prompt foi entregue ao agente do LA Report: achar o tempo, consertar sem mudar o contrato, pior caso < 2 s. O TOM não muda: já repete uma vez e não afirma sem a fonte.
- Observar em produção: a primeira resposta real a um cardápio de tarefa (`[Engine] ... (cardapio_tarefa)` no log e `TASK_UPDATE executed cardapio_*`).
