# Allowlist do verificar-bundle.sh — literais de alta entropia ACEITOS no bundle público.
#
# Formato: <sha256 do literal>  # motivo
#
# Regra: um literal só entra aqui com motivo escrito, depois de alguém decidir que ele é
# público POR DESENHO. O hash não revela o valor, mas a linha registra a decisão.
# Para obter o hash de um valor sem expô-lo:  printf %s "$VALOR" | sha256sum
#
# =========================================================================================
# CLASSIFICAÇÃO DE 2026-08-28 — 9 achados no bundle de produção, identificados pelo
# CONTEXTO em que aparecem no código minificado (nenhum valor foi impresso).
# =========================================================================================

# --- Constantes de biblioteca (não são segredo) ------------------------------------------
775ad11d37eebfe985acd54acdaa5d2c40181421389044b87d29d62182a43e6c  # alfabeto base64: 64 chars, 64 DISTINTOS. Aparece logo apos AuthInvalidJwtError, no @supabase/auth-js. Entropia 6.00 = maxima possivel para 64 simbolos, assinatura de alfabeto, nao de segredo.

# --- Códigos de erro do WebAuthn (biblioteca de passkey) ---------------------------------
f66a0a5dfa54632f130cc05b841419b1c3ba5ed3003d12d9759e5abcaebc00c3  # code: apos "credentials were required but no available authenticator supported it"
90c7067e596ca4526c22668a4ebfe8223ab090a70cebb3fe0665bdba59792ec5  # code: apos "...ation was required but no available authenticator supported it"
2c2456847caa5248a2170a33d29de94ce88099debd77b57ab716e4992a9b7459  # code: apos "...any of the specified pubKeyCredParams algorithms"
df5043ebd715ff803040af7350724d1620ef875d79e790ea6ad4600401fddbbc  # code: apos "The authenticator was previously registered"
f423747445e8d214a8837ddcdf83820cb30a5f1861c1e0f08b4aa863829c26b2  # code: apos "...during automatic registration but it could not be performed"

# --- Código da própria aplicação ---------------------------------------------------------
473e6b0742dfb35d5d6f9f9083b6e880801a60382948892a39c94a939f850609  # fragmento de select do PostgREST: "...checklist_completions!inner(reference_date, op_checklists(name))"

# --- Chave anon do Supabase --------------------------------------------------------------
# ATENCAO: publica POR DESENHO (vai no cliente para o PostgREST aplicar RLS). Ela so e
# inofensiva ENQUANTO a RLS estiver correta. Em 2026-08-28 ela da SELECT em 5 tabelas sem
# RLS — este hash so deve permanecer aqui depois que o P0-1a estiver aplicado e verificado.
79bb4b2ae5d832fac571c4549c2d756b786531b4bcad30b8fb636c6221e39b77  # JWT anon, 208 chars, adjacente a const com a URL do projeto Supabase

# --- Achados novos, revelados pela varredura dos 47 chunks (v2.3) -------------------------
# Ate a v2.2 o detector lia so `index-*.js`. Ao passar a varrer os 46 chunks lazy, apareceram
# tres literais que nunca tinham sido examinados. Classificados por PROPRIEDADE, sem imprimir
# valor: contando caracteres distintos e decodificando o header/claims do JWT.
02a3b5155093b03b36e4776cb1618f16d49c71669f4cd1a621fa5ab51864cb71  # 64 chars, 64 DISTINTOS = alfabeto base64 de biblioteca. Segredo nao tem todos os simbolos distintos.
459efb5d996d6127e5d012f1b8677ca47189e04a0ca150d5b9258780dab7458f  # 66 chars, 66 DISTINTOS = alfabeto base64url com padding. Mesma assinatura.
4527b37ad4b51da92a7378d309508b6a62b1d198ce5d5d3d963d35d08480022f  # JWT anon do LA REPORT (ref=ouqwbbermlzqqvtqwlul, role=anon). Publico por desenho, igual a anon do TOM.
#
# ⚠️ A anon do LA Report tem a MESMA condicao da anon do TOM: so e inofensiva enquanto a RLS
#    daquele projeto estiver correta. O P0-1a auditou e fechou o projeto do TOM
#    (cesnbnrynvxvgdhfmaua). O LA Report (ouqwbbermlzqqvtqwlul) NAO foi auditado — item aberto,
#    fora deste pacote. Este hash fica aqui porque a chave e publica por construcao, nao
#    porque o projeto dela esteja provado seguro.

# --- Fragmentos de CÓDIGO minificado, não literais (v2.5) ---------------------------------
# A v2.4 filtrava estes com a regra "sem nenhum dígito = não é credencial". O laudo derrubou a
# premissa e está certo: segredo NÃO é obrigado a ter dígito. Regra ampla que descarta uma
# classe inteira é bypass esperando acontecer — então eles voltam a ser examinados e são
# classificados AQUI, um a um, por hash.
#
# O que são: o extrator casa de uma aspa que FECHA uma string até a aspa que ABRE a seguinte.
# Em JS minificado não há espaços entre elas, então o trecho no meio é código. O contexto de
# cada um está anotado — foi assim que classifiquei, sem imprimir o valor.
#
# ⚠️ Estes hashes MUDAM quando as libs são reconstruídas (nomes minificados mudam). Quando
#    isso acontecer eles voltam como achado não classificado — e isso é o comportamento certo:
#    obriga a reclassificar em vez de confiar numa regra que já provou ser furada.
c6bc5480994a0ab13ed7c25acd10ff7f5b87686f524c1051feb9256e189849e3  # index-*.js, 47 chars, 20 distintos. Contexto: `...!==t)&&(n==null?t.defaultValue=""` … `""+n&&(t.defaultVa` — React DOM, entre duas strings vazias adjacentes.
08fd4de1d0d45eb0b4f76151c528079333f7a1f2f1eea6f0c274fcbd0422c22e  # CartesianChart-*.js, 53 chars, 19 distintos. Contexto: `return t==="axis"?r==="click"` … `"click"?e.itemInte` — ternário da lib de gráficos.
2c4f2f7cc4c6fe14e37b7eaf2df8aaf4b7a72806dc3eef5852da39b3795e84de  # CartesianChart-*.js, 69 chars, 22 distintos. Contexto: `n=Jr(e);return t==="axis"?r==="hover"` … `"hover"?n.itemInte` — mesmo ternário, ramo hover.

# =========================================================================================
# NÃO ALLOWLISTADO — É O P0-4
#
#   08dabf36fe915d8305101cb63f636b86e23c8a731260c9b7306db8e0fae870fd
#   64 chars, 16 caracteres distintos = hexadecimal (formato de `openssl rand -hex 32`).
#   Aparece como `const hs="",ms="<valor>"` — o par bate exatamente com tomEngine.ts:
#     const TOM_BASE       = import.meta.env.VITE_TOM_API_BASE       || ''   -> hs=""
#     const INTERNAL_SECRET= import.meta.env.VITE_INTERNAL_API_SECRET || ''   -> ms="..."
#   É o VITE_INTERNAL_API_SECRET, única proteção de 19 endpoints /internal/*.
#
#   Este hash NÃO deve ser allowlistado. Ele deve DESAPARECER do bundle — trocando o
#   mecanismo (JWT do Supabase ou proxy server-side), rotacionando e rebuildando.
#   Enquanto ele aparecer, o verificar-bundle.sh reprova, e deve reprovar.
# =========================================================================================

# --- Achados revelados pelo caminhamento ate PONTO FIXO (v2.6, laudo v2.5 bloqueador 3) ----
# A v2.5 casava referencia de chunk so no formato `assets/<nome>.js`. Medido em 29/08: o
# bundle de producao referencia UM asset na pagina (o index) e ele carrega os outros como
# `./XxxPage-hash.js` -- sibling, sem `assets/` no caminho. O scanner lia 1 arquivo de 53.
# Com o ponto fixo, 53 assets / 3,6 MB, e apareceram 11 literais que nunca tinham sido vistos.
#
# TODOS os 11 vem do MESMO chunk (GovernancaPage-*.js, que carrega CodeMirror e
# react-markdown) e foram identificados pelo CONTEXTO, com o valor substituido por
# <VALOR len=N> -- nenhum caractere impresso. Nao ha credencial entre eles:
#   * tabelas LR serializadas do parser do CodeMirror (`$TX!d$TX$Y...`, `!ta#r!ta#s...`);
#   * chaves de CSS-in-JS do autocomplete (`.cm-completionInfo.cm-completionInfo-*`);
#   * ids do mapa de deprecacao do react-markdown (`remove-...`, `allowNode`, `linkTarget`);
#   * um nome de propriedade do highlighter e uma URL de documentacao concatenada.
11ac5e4201cf4871cc106e1022b2ec19be416c5dfa3e418a4b28353b296a62d2  # tabela LR do parser CodeMirror (78 chars, entropia 4.31)
ac27df9ac29020edf90370a5b63d953879bc6c4134fb5ded3608d2c4bd61c55f  # nome de propriedade do highlighter (44 chars) em `PrivatePropertyName:x.special(...)`
543f6208e5c7c95f366c6161a14eebf86a743304956bc2dcd8f3ea01a6f3341f  # tabela LR do parser CodeMirror (94 chars, entropia 3.97)
4cc6f34df19f9b83035a0e97ea0084a16b541cb6dff8b531499844928760a05c  # seletor CSS do autocomplete do CodeMirror (`.cm-completionInfo-*-narrow`)
025e2fd329f437989d7e4e6ed313146c44c93f9f82da776002b5b9505fbe23e3  # URL de doc concatenada em `t.url=Yp+<valor>` (hast-util-to-jsx-runtime)
01ea9475a0a5abc66ade18795aa2dea6bc549a996f9c953933c64cf9ea534ee7  # seletor CSS do autocomplete do CodeMirror (`-left`)
d7932aa83509ed93a3b5ec57cd46b75a308d97340b50b487a83c51adce075d03  # seletor CSS do autocomplete do CodeMirror (`-right`)
c32755fc5987770c43f4bb9bd7e0496bbcc51cb0920c57f55ef41f1c4753c447  # id do mapa de deprecacao do react-markdown (`allowNode` -> `allowElement`)
139069a8e39042c6b8339e7aabe8c8f1afa8dc640d466c6e045d8fa52ebd7314  # seletor CSS do autocomplete do CodeMirror (whiteSpace/pre-line)
564d44b4f3359bfb7c398c5664a5c45f3483d7980919c50c79da02383441364e  # tabela LR do parser CodeMirror (49 chars)
68571f580d7e5330286b45ca0266a4fdb3e395ca16a4f8587b6119083164c13d  # id do mapa de deprecacao do react-markdown (`includeNodeIndex`)
