# 2026-09-04

## Credenciais por WhatsApp — fatia 3 em produção e dois bugs reais (~14h–18h BRT)

Primeiro dia com a escrita de credencial no ar. Os dois testes E2E do Hugo derrubaram duas
falhas que a suíte não pegava — as duas fora dos módulos puros, no acoplamento com o engine.

### Bug 1 — leitura: TOM respondeu do conhecimento próprio (RESOLVIDO, `c5129f2c`)

Hugo pediu o refresh token do Google e o TOM devolveu um endpoint OAuth genérico em vez de
dizer que não tinha. Causa: a instrução do prompt dizia "emita o marker se a pessoa perguntar
por acesso **e você não tiver essa informação no contexto acima**". O modelo *tinha* a
informação (do mundo), então a condição lia falso e ele nunca se viu desobedecendo o "NÃO
invente URL". Conserto: removida a cláusula, adicionado **"Emita o marker mesmo achando que
já sabe a resposta"**, e o 2º passe agora diz explicitamente para responder "não está
cadastrada" quando o item não vier no bloco.

### Bug 2 — escrita: o engine mandou o TOM pedir os dados de novo (RESOLVIDO, `e06b928c`)

Segundo cadastro. O TOM respondeu `Vou cadastrar: ... Confirma?` **em prosa, sem
`<<CREDENCIAL_ACTION>>`**. Sem marker o executor determinístico não roda: nenhuma intent
`credencial_write` nasce, nada é gravado — e a senha ainda vai em texto claro na tela, porque
quem escreveu a confirmação foi o modelo (que não mascara) e não o executor (que mascara).

No turno seguinte o "isso" caiu no auto-resolve **genérico** ([engine.js:10676](../src/engine.js#L10676)),
cujo ramo sem payload concreto manda, literalmente:

> "assuma que não conseguiu registrar e **peça pra pessoa repetir o pedido com os detalhes**"

Ou seja: a frase "me manda os dados de novo" **foi ditada pelo engine**, com os dados dois
turnos acima e visíveis pro modelo. Procurei a string no repo justamente pra descartar
alucinação — não existe em lugar nenhum, o modelo obedeceu à instrução.

**Conserto em duas camadas, nenhuma delas incomodando a pessoa** (pedido explícito do Hugo:
"ele tem que ser esperto e evitar ficar pedindo resposta do usuário toda vez que ele erra"):

1. **Retry no mesmo turno** ([engine.js](../src/engine.js), antes do executor). Gate
   determinístico novo — [src/lib/credencial-retry-gate.js](../src/lib/credencial-retry-gate.js) —
   reconhece a fala de escrita sem marker; um mini-prompt converte em marker e o executor
   assume dali: mascara os valores, abre a confirmação e apaga a inbound. **A fala com a senha
   em claro nem chega a ser enviada.** Guardas: turno de LEITURA não entra
   (`_credenciaisNoTurno`), não-admin não gasta chamada, não encadeia com o `AUTO_RETRY` de
   tarefa, e qualquer erro deixa a resposta como estava.
2. **Auto-resolve** (rede de segurança). `_liberaCredencial` entra **antes** e **veta** o
   create-gate — `podeLiberarCriacao` casa com "vou registrar", então uma proposta de
   credencial podia virar **tarefa nova com o segredo dentro do título**. A regra passa a
   mandar reproduzir a própria proposta, nunca pedir de novo.

Achado colateral fechado no mesmo commit: o `question_text` gravado em `marker_logs` ia cru —
e `raw_excerpt` é transmitido pelo relatório das 7h no WhatsApp dos diretores. Agora passa
pelo redator.

**Provas:** 23 do gate puro, 9 do bloco de retry **extraído do engine real**, 3 de fonte da
camada 2, 1 do WIRING de TaskHonesty. Falsificação feita: sabotar a linha que anexa o marker
derruba o teste (69/70). Suíte: 3843/3855 — as 12 falhas restantes já eram vermelhas em HEAD
(dependem de env).

### Deploy

- `e06b928c` em `origin/main`; `src/engine.js` e `src/lib/credencial-retry-gate.js` por SCP na
  VPS + `pm2 restart tom`. TOM subiu limpo às 17:50.
- ⚠️ **A VPS tem trabalho de outra sessão que só existe lá:** commit local `9c30b195`
  (`fix(memoria-grupo)`, não está no origin) e modificações não commitadas em
  `group-chat-*`/`optimistic-confirm.js`. **Não rodar `git reset --hard origin/main` na VPS**
  — destruiria isso. Por isso o deploy foi por SCP dos dois arquivos, depois de conferir por
  md5 que o `engine.js` da VPS era idêntico à minha base (`c5129f2c`).

### Pendências

- 👁️ **OBSERVAR** — o retry (`CREDENCIAL_RETRY`) e a camada 2 (`CONFIRM_CREDENCIAL_ALLOWED`)
  só provam valor com tráfego real. Checagem:
  ```sql
  select marker_type, result, reason, count(*), max(created_at)
  from marker_logs
  where marker_type in ('CREDENCIAL_RETRY','CONFIRM_CREDENCIAL_ALLOWED','CONFIRM_NOEXEC')
    and created_at > '2026-09-04'
  group by 1,2,3 order by 5 desc;
  ```
  **Sucesso:** `CREDENCIAL_RETRY executed:marker_recuperado` aparecendo e nenhum cadastro
  perdido. **Fracasso:** `rejected:sem_marker_valido` recorrente (mini-prompt fraco) ou
  `CONFIRM_NOEXEC` com pergunta de credencial (os dois gates erraram). Concluir só com ~5+
  ocorrências, não com uma.
- **Resíduo conhecido, não fechado:** quando a camada 1 falha em converter, o registrador
  genérico de fim de turno grava a proposta do TOM (com os valores em claro) em
  `pending_intents.question_text`. Redigir ali quebraria a camada 2, que precisa desse texto
  pra reproduzir a proposta. Fica registrado — decidir se vale um campo separado.
- **E2E que faltam:** anti-duplicata (`cadastra a conta do Canva: ...` deve bater na
  `Canva — criativos`), recusa a não-admin, e repetir a pergunta de leitura depois do
  `c5129f2c`.
- **Credencial de teste** `Google Ads API - LA Music` segue no banco — apagar exige OK
  explícito do Hugo (dado de produção).

## LAOR-2 — o que o time enxerga vira visível e editável (~18h BRT)

Hugo pediu ao TOM "quais links você tem" e recebeu as 46 com tudo — **correto**, ele é
`is_system_admin`. O que o episódio revelou é outra coisa: **não havia como saber que o time
inteiro só enxerga 3 delas**, nem como mudar isso.

`visivel_tom` existia como coluna desde 07/08 e já decidia o escopo público dentro da RPC, mas
**não tinha controle em lugar nenhum** — zero ocorrências em `web/src`, nada no TOM. As 3
ligadas vieram de migration e congelaram. E toda credencial que o TOM cadastra nasce `false`
(hardcoded no insert de `upsert_credencial`).

### Item 1 — o TOM mostra o estado

- **Migration** `20260904_credenciais_expor_visibilidade`: `get_credenciais_para` volta a
  devolver `visivel_tom` no escopo admin. Precisou `DROP` — `RETURNS TABLE` não muda em
  `CREATE OR REPLACE`. Escopo público **inalterado**.
- `formatCredencialAdmin` marca as públicas com 🌐.
- **O rodapé é determinístico.** O 2º passe reescreve o bloco e pode comer a marcação, então o
  engine cola *depois* uma linha com os nomes por extenso ("2 de 46: X, Y"). Só em listagem —
  `rodapeVisibilidade` exige ≥2 nomes citados na resposta, senão uma pergunta pontual ("qual a
  senha do Canva?") carregaria o rodapé em todo turno.

### Item 2 — dá pra editar

Checkbox na Governança (nova + detalhe) e 🌐 na listagem, que é onde a curadoria das 46
acontece de fato.

⚠️ O escopo público da RPC é uma **conjunção**: `visivel_tom AND status='ok' AND url_ref not
null`. Marcar sem link é **no-op silencioso**. [visibilidadeTom.ts](../web/src/lib/visibilidadeTom.ts)
espelha a condição: sem link a caixa fica desabilitada e o hint diz o porquê; com status fora
de OK avisa que ainda não vale. O submit grava `false` quando não há link, pra a flag não
"reaparecer" sozinha se alguém puser link depois.

### Item 4 — as duas portas passam a ser vigiadas

| | TOM (WhatsApp) | PWA (Governança) |
|---|---|---|
| Porta | `collaborators.is_system_admin` | `role = 'director'` (ProtectedRoute + 4 policies de RLS) |
| Como lê | RPC `get_credenciais_para` | `.from('governance_credentials')` **direto** |
| Níveis | 2 (`visivel_tom`) | tudo ou nada |

São flags **independentes** e nenhuma consulta a outra: promover alguém a `director` concede
leitura das senhas na Governança sem ninguém tocar em `is_system_admin`. Hoje coincidem
(Hugo, Luciano, Anne Susan). Novo check `portas_credenciais` no health-check das 7h, validado
contra o banco real na VPS: `ok — 3 pessoa(s) com acesso total`.

**Não sincroniza automático de propósito** — as flags significam coisas diferentes (papel na
empresa × acesso a segredo) e casar uma na outra concederia acesso sozinha.

Item 3 (TOM avisar no cadastro que nasceu restrita) **recusado pelo Hugo**.

### Provas e deploy

- 8 novas no formatador, 9 em `portas-credenciais` (com WIRING contra o módulo virar órfão no
  health-check), 5 no `visibilidadeTom` do web. Backend 3903/3916.
- ⚠️ As falhas de suíte subiram de 12 pra 13 — a nova (`group-chat-watcher.test.js`) veio nos
  6 commits da outra sessão e é da mesma classe das outras: `Variavel faltando: SUPABASE_URL`.
  Nenhuma é regressão.
- `tsc --noEmit` limpo, `vite build` ok. Commit `2f5b4b1d`, rebaseado sobre os 6 commits da
  outra sessão (sem sobreposição de arquivo — conferido por `git diff --name-only`).
- VPS: SCP dos 4 arquivos + `pm2 restart tom` às 18:19. Conferi por md5 que `engine.js`,
  `credenciais-format.js` e `health-check.js` da VPS eram idênticos à minha base antes de
  sobrescrever.

### Pendências

- **Curadoria das 46 é decisão do Hugo.** Candidatos plausíveis a público: Canva — criativos,
  Drive fotos/vídeos, os dois OneDrive de acervo, Dashboards de leads. ⚠️ Drive, OneDrive e
  Sheets são **links de capacidade** (quem tem o link entra) — marcar não vaza campo, mas vaza
  o acesso. As chaves de API, o WordPress App Password e a VPS **não abrir nunca**.
- 👁️ **OBSERVAR** — o check `portas_credenciais` no relatório das 7h. Sucesso: linha `ok` com
  os 3 nomes. Fracasso: `warning` nomeando quem divergiu.
- **Autoria no histórico segue ABERTA** (ver seção do bug 2): `system.js:4039` devolve todo
  outbound como `role: 'assistant'`, inclusive o que o **engine** escreveu. É a causa do TOM
  imitar a forma da mensagem do executor sem o marker. O retry de hoje é a rede, não a causa.

## LAOR-3 — duplicata de credencial: identidade é o sistema, não o segredo (~18h40 BRT)

Hugo mostrou uma conversa das 15:08. Ele pediu pra cadastrar a conta do Google Ads e o TOM
respondeu:

> Antes de criar: já existe algo parecido.
> 1. Domínio — registro.br — mesmo valor no campo Senha
> 2. Mila Openclaw — mesmo valor no campo Senha
> 3. Mila Supabase — mesmo valor no campo senha
> Quer atualizar uma dessas (responde o número) ou criar assim mesmo (responde *criar*)?

Nenhuma das três tem relação com o Google Ads — o que elas compartilham é uma **senha
reaproveitada**. A pergunta ficou de pé e **a credencial nunca foi criada** (conferido: a única
linha nova de 04/09 é a de teste das 17:20). Um falso positivo bloqueou um cadastro legítimo.

⚠️ E o risco não era só o bloqueio: responder **"1"** levaria os dados do Google Ads para dentro
do registro.br. Não seria silencioso (a 2ª confirmação do fix C-1 mostra o resultado antes de
gravar), mas a pergunta **afirma uma falsidade**.

### Causa

O ramo ALTA casava **qualquer** valor de campo repetido. O comentário no código dizia
"(e-mail/login ja cadastrado)" — a intenção era campo de identidade — mas o loop aceitava todo
campo. Varrendo a tabela: `Custo/mês` igual em 4, `Dispositivo` em 4, `Integração` em 5,
`Usuário=admin` em 2, senhas reaproveitadas em 4 e em 5. **Dois cadastros que custam o mesmo
por mês eram "alta duplicata".**

### O que mudou

- Só **campo de identidade** participa: rótulo de identidade **e** valor que identifica
  (tem `@`, ou ≥6 chars fora da lista de lixo). Custo/Dispositivo/Integração saem pelo rótulo;
  `admin` sai pelo valor.
- **Campo sensível não gera duplicata.** Virou `acharReusoDeSegredo`, que o engine mostra como
  **aviso dentro da confirmação normal** — a pessoa fica sabendo do reúso e cadastra no mesmo
  passo. O aviso nunca carrega o valor, só o nome da credencial e o rótulo.
- Identidade sozinha virou **média**, não alta. Evidência da base: `Chave openai` e
  `Gmail — Escola de Música LA` têm o mesmo e-mail e são coisas diferentes. Vira alta só com
  host do link ou serviço batendo junto.
- Nome parecido ganhou tamanho mínimo (4) — "Sol" casava com quase tudo.
- No update o aviso ignora o próprio alvo; no delete não aparece.

### A prova (rodada na VPS, só leitura)

Rodei a regra antiga e a nova contra as 46 reais. Rodei **na VPS de propósito**: assim nenhum
valor de campo sai do servidor — só agregados e nomes voltam.

| | Antes | Depois |
|---|---|---|
| Pares acusados | **82** | **24** |
| Pares novos | — | **0** |
| Credenciais que ganham aviso de reúso | — | 11 |

Os 58 que sumiram: 19 por `Senha`, 16 por `Integração`, 14 por `Dispositivo`, 5 por outras
senhas, 2 por `Custo/mês`, 2 por e-mail marcado como sensível.

**A prova reprovou uma ideia minha.** Eu tinha posto "mesmo host sozinho = média". Ela gerava
**12 pares novos**, e os piores eram hosts genéricos — três contas de Gmail diferentes viravam
"parecidas" só por morarem em `mail.google.com`. Removi, com teste fixando a decisão pra não
voltar sem nova prova. O caso legítimo (recadastro do mesmo sistema) já é coberto por
host+identidade = alta.

### Provas e deploy

- 25 testes no módulo puro (incluindo o caso real, os campos que não identificam, `admin`, o
  nome curto e o host sozinho) + 5 no executor extraído do engine real.
- Achado do harness: `credenciais-format` não estava stubado no `credencial-engine.test.js` —
  o require falhava dentro do try/catch do executor e 14 testes ficavam vermelhos por engano.
  Corrigido.
- Suíte 3918/3931; as 13 falhas seguem sendo as de env.
- Commit `e13511aa`, VPS por SCP + restart às 18:38.

### Pendências

- 👁️ **OBSERVAR** — cadastrar de novo a "conta do ADS Google" e conferir que agora vai em um
  passo, com o aviso de reúso embaixo do resumo.
- ⚠️ **Reúso de senha é um problema real, não só ruído de detecção:** 4 cadastros compartilham
  uma senha (inclusive **Domínio — registro.br** — perder essa é perder o domínio) e 5
  compartilham outra (Cloudflare, TurboCloud, WordPress LPs, Stape.io e o Gmail de backup das
  LPs — uma senha derruba a stack inteira das landing pages). **Decisão de segurança do Hugo**,
  não conserto de código; fora do escopo do LAOR-3 por decisão dele.
